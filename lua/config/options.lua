-- NOTE: These options here will be set in vim.opt
return {
    clipboard = "unnamedplus",

    -- opt.relativenumber = true
    nu = true,
    cursorline = true,
    termguicolors = true,

    -- Text
    expandtab = true,
    softtabstop = 4,
    shiftwidth = 4,
    tabstop = 4,
    smartindent = true,
    wrap = false,

    -- Highlighting
    -- opt.spell = true,

    -- Search Options
    hlsearch = true,
    ignorecase = true,
    smartcase = true,
    incsearch = true,
}
